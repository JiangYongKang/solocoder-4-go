产物不满意：
1. models.go 的 isDateWithinDays 函数，只通过 `dateOnly(expiryDate).Before(cutoff)` 判定到期日是否在截断日期之前，缺少对已过期日期的下限排除。当 GetExpiringBatches 方法查询"近N天到期"批次时，如果一个到期于一年前但状态尚未被 UpdateBatchStatus 更新为 Expired 的批次存在，isDateWithinDays 会对其返回 true，导致该批次出现在临期告警列表中。触发节点是调用 GetExpiringBatches 时未同步状态的老旧过期批次；实际行为是过期批次被当作临期批次返回；业务影响是临期告警列表混入已过期的批次，让调用方无法区分真正即将到期的和早已过期的库存；根本原因是 isDateWithinDays 缺乏 `!isDateExpired(expiryDate, asOf)` 的下限保护；正确做法是在该函数中增加"到期日期必须大于等于判定日期"的约束，确保只返回未过期但在未来N天内到期的批次。
2. drugtrace.go 的 OutboundFIFO 方法在第 236 行对扣减循环中每个待处理批次再次调用 isDateExpired 检查过期，而同样的检查在第 196 行的批次筛选循环中已经使用同一个 now 基准值执行过了。触发节点是 OutboundFIFO 进入扣减循环处理 availableBatches 时；实际行为是对筛选后已确认未过期的批次重复判定过期；根本原因是模型在实现统一时间基准时没有意识到筛选循环已经将过期批次排除出 availableBatches 集合，导致第 236 行至第 239 行的重复检查成为永不触发的死代码；正确做法是删除扣减循环中的冗余 isDateExpired 检查和对应的 status 更新逻辑，availableBatches 已保证所有条目均未过期。
3. drugtrace.go 的 GetExpiringBatches 方法在遍历批次时仅跳过 `BatchStatusRecalled` 状态的批次，但没有排除 `BatchStatusExpired` 状态的批次，而该方法同样使用 isDateWithinDays 做日期范围的判定。当存在状态为 Expired 的批次时（例如 InboundBatch 入库时直接标记为过期），isDateWithinDays 对已过期日期也返回 true，导致这些已过期批次同样出现在临期列表中。触发节点是存在 Expired 状态批次且调用 GetExpiringBatches 时；实际行为是已过期批次被错误归类为临期批次返回；业务影响是调用方同时从 GetExpiredBatches 和 GetExpiringBatches 两个查询中看到相同的过期批次；正确做法是在 GetExpiringBatches 的循环条件中增加 `batch.Status == BatchStatusExpired` 的过滤，仅检查 Status 为 Normal 的批次的到期日期。
过程不满意：
1. 模型在 session-cot-2.md 的开头，刚收到提示词后未经任何代码分析就直接调用 todo_write 创建了 7 项子任务计划，之后才开始读取 drugtrace.go 和 models.go 的文件内容。正确的做法是先分析需求文档和现有代码结构，理清三个提示词问题的具体影响范围和修复方案，再基于分析结果制定对应的执行计划。这种"先规划再分析"的顺序违反了标注规范中的"不能在刚收到任务时就直接创建 TodoList，应先分析问题再规划"要求，导致任务的粒度划分与实际修改节奏脱节。
2. 模型在编辑完 drugtrace_test.go 添加新测试函数后，没有先检查代码的正确性就直接运行 `go test`，结果测试文件因缺少 `fmt` 包的 import 导致编译失败。修复 import 后再次运行，又因为测试代码中 `_ = s.AddDrug("DRUG001"...)` 的返回值未使用产生编译错误。两次基础的编译问题说明模型在代码编辑完成后缺乏自检步骤，每次都依赖编译器来发现基本错误。正确做法是完成编辑后先自查 import 是否齐全、变量是否已使用，确认语法无低级错误后再运行测试，提升开发效率和测试成功率。
