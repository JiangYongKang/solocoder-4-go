你实现的住院床位调度模块存在以下问题需要修复：
1. matchPatientCondition 方法中对儿科患者的床位匹配逻辑存在冗余和边界遗漏。当儿童患者（年龄<14）同时满足 `criteria.BedType == ""` 和 `bed.Type == BedTypePediatric` 以外的另一个匹配条件时，该非儿科床位会因为第一个 if 分支的儿科检查而被错误排除。另外，当患者标记为 "surgery" 但确实需要 ICU 支持时，当前逻辑强制限制了手术患者只能分配手术间或普通床位，无法覆盖术后需要 ICU 监护的场景。需要重新梳理患者条件与床位类型的匹配规则，确保多层条件组合时的判定逻辑无歧义。
2. 修复后需要为该模块补充患者条件与床位类型多条件组合匹配场景的单元测试，覆盖儿童患者无指定类型、手术患者需 ICU、传染病患者无隔离床位回退等边界。
3. 运行 go test ./internal/bedalloc/ -v，需要让所有测试通过。
